import React from 'react';
import styled from 'styled-components';
import PropTypes from 'prop-types';

const FilterWrapper = styled.div``;

const Filter = () => {
  return <FilterWrapper>Filter</FilterWrapper>;
};

Filter.propTypes = {
  //
};

export default Filter;
